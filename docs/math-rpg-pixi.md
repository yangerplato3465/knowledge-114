# 數學勇者 · PixiJS 導入評估

這份是**做 Pixi 之前的盤點與路線選擇**，不是實作規格。寫於 2026-08-26，當時一行 Pixi 都還沒寫。

- 素材處理流程（角色去背 vs 特效轉檔）→ 看 `.claude/math-rpg-fx.sh` 和 `.claude/math-rpg-keyer.ps1` 的檔頭註解，寫得很完整。
- 劍氣的軌跡、時序、爆擊怎麼運作 → 看 `assets/js/math-rpg.js` 的 `playSlash` 與 `SLASH_IMPACT_DELAY` 註解。
- **這份講的是「哪些東西值得搬進 Pixi、哪些不該搬、以及搬之前要先決定什麼」。**

---

## 0. 開始之前

**這台機器沒有 Node.js**，不能 `npm install pixi.js`。用 CDN：

```html
<script src="https://cdn.jsdelivr.net/npm/pixi.js@8/dist/pixi.min.js"></script>
```

功能不打折。實測 2026-08-26（`@8` 解析到 v8.20.0）：

| | 大小 |
|---|---|
| gzip 傳輸 | **231 KB** |
| 解壓後要解析的 JS | 818 KB |
| 目前整包遊戲圖檔 | 約 670 KB |

**載入量直接翻倍。** 這遊戲要在學校電腦跑，導入前確認兩件事：
1. 教室網路 —— 第一次載入會明顯變久（之後有快取就還好）
2. 舊機器的 JS 解析時間 —— 818KB 要 parse，低階機可能多花數百毫秒

要瘦身得自己 build 只含需要的模組，但那需要 Node.js。

---

## 1. 現況盤點（Pixi 導入前）

DOM/CSS 能做的其實已經做得相當滿，所以下面每一項標的「值得度」都是**相對於現況的增量**，不是絕對值。

- **39 個 CSS keyframes**、**8 個 fx 函式**、**6 個畫面**
- 粒子全部是 CSS 動畫的 `<span>`：一次命中只噴 11 顆碎片（爆擊 20 顆）
- 已經做得不錯、**不需要動**的：血條殘影條、兩段計時警示（10 秒／5 秒）、地圖節點、升級卡片動畫

fx 函式清單（`assets/js/math-rpg.js`）：
`impact` `shakeScreen` `stageFlash` `fxSpawn` `fxSlash` `fxRing` `fxSparks` `fxHeal` `fxConfetti` `showDamage`

---

## 2. 可以改的項目

### A. 戰鬥特效 — Pixi 收益最大的區塊

| # | 項目 | 現況 | Pixi 之後 | 工 | 值得 |
|---|---|---|---|---|---|
| 1 | 劍氣拖尾 | 無 | 飛行途中持續灑光點，軌跡本身變可見 | 小 | ★★★ |
| 2 | 命中爆散 | 11 顆 DOM span | 300+ 加亮粒子，重疊處爆白 | 小 | ★★★ |
| 3 | 衝擊波 | CSS 圓環放大 | 位移著色器，畫面真的被推開 | 中 | ★★★ |
| 4 | 爆擊演出 | 雙重震動＋大數字 | 徑向光束＋色差＋頓幀 | 中 | ★★★ |
| 5 | 怪物死亡 | 18 顆碎片＋CSS 淡出 | 剪影碎裂成數百片飛散 | 中 | ★★ |
| 6 | 勇者受傷 | 紅色閃屏 | 血花／火花＋角色殘影 | 小 | ★★ |
| 7 | 蓄力詠唱 | 完全沒有 | 粒子往劍上聚集＋垂直光刃素材（見下註） | 中 | ★★ |

> 第 7 項的垂直光刃 `charge.webp` 已從 repo 刪除（技能系統還沒做，留著只是孤兒素材）。
> 要用時把 `.claude/math-rpg-fx.sh` 裡 `slash2:charge:0` 那行取消註解再跑一次，來源 `slash2.png` 仍在 Downloads。

### B. 角色與場景 — 需要把角色也搬進 Pixi

| # | 項目 | 現況 | Pixi 之後 | 工 | 值得 |
|---|---|---|---|---|---|
| 8 | 角色改 Pixi Sprite | CSS background-image | 能上著色器：受傷閃白、殘影、溶解 | 中 | ★★★ |
| 9 | 場景視差 | 單張靜態背景 | 背景分層，衝刺時遠近位移不同 | 中 | ★★ |
| 10 | 環境粒子 | 無 | 每關不同：草原飄花瓣、洞窟落塵、火山飄火星、魔王城暗霧 | 小 | ★★★ |
| 11 | 地面陰影 | CSS 漸層橢圓 | 隨跳躍縮放、隨光源偏移 | 小 | ★ |

### C. 介面 — 多數不划算

| # | 項目 | 判斷 |
|---|---|---|
| 12 | 血條 | **不要搬。** 殘影條已經做得很好，Pixi 重做只是換一種寫法 |
| 13 | 傷害數字 | 可搬成 BitmapText 做更誇張的彈跳，但要處理中文字型載入。★ |
| 14 | 升級卡片 | **文字排版留 DOM**，可以在卡片後面墊一層 Pixi 光效背景 ★★ |
| 15 | 地圖節點 | 連線做流動光效、打倒時節點爆開 ★★ |
| 16 | 題目／選項按鈕 | **絕對不要搬。** 文字選取、鍵盤操作、無障礙全部會壞掉 |

### D. 過場與結算

| # | 項目 | 現況 | Pixi 之後 | 值得 |
|---|---|---|---|---|
| 17 | 關卡切換 | 直接換圖 | 刷屏／溶解轉場 | ★★ |
| 18 | 勝利彩帶 | 70 顆 DOM | 2000 顆帶重力與翻轉 | ★★ |
| 19 | 失敗畫面 | CSS 淡出 | 灰階著色器漸染 | ★ |

---

## 3. 架構上的關鍵決定

**如果只把 Pixi 當成疊在上面的特效層，粒子永遠只能在角色的「前面」或「後面」，不能穿插。**
劍氣從怪物身後穿到身前、碎片繞著角色飛，這類效果做不出來。

第 8 項（角色搬進 Pixi）是門檻 —— 過了這關 A 和 B 的所有項目才能互相疊加。
但它也意味著 `.sprite` 那整套 CSS 動畫要改寫成 Pixi ticker 動畫：
`lunge` `hurt` `die` `breathe` `floatIdle` `heavyIdle` `spawn` `shadowBreathe`

### 路線一：疊加層（保守）
只做 **1、2、6、10、18**。DOM 完全不動，隨時可以拿掉 Pixi。約能拿到七成的視覺提升。

### 路線二：戰鬥區全面 Pixi（徹底）
把 `#battle-stage` 整個換成一張 Pixi canvas，角色／劍氣／粒子／背景全在裡面。
血條、題目、按鈕留在 DOM。A + B 全部解鎖。

### 建議
**先做路線一的 1、2、10**，跑起來看實際感覺，再決定要不要投入路線二。

理由：路線二會動到 `.sprite` 那整套已經調得很順的動畫，如果做完發現手感不如現在的 CSS 版本，回頭成本很高。
路線一的 1、2、10 完全不碰現有邏輯，做完就知道 Pixi 在實機上的效能和觀感。

---

## 4. 導入時要小心的地方

- **劍氣的軌跡是 JS 現場量測的**（`playSlash` 量攻守雙方 sprite 中心）。若角色搬進 Pixi，這段要改成讀 Pixi 物件的座標，不能再用 `getBoundingClientRect`。
- **時序有三個數字綁在一起**：`slash.at = 0.26`、CSS `slashFly` 的 0.5s／53%、`SLASH_IMPACT_DELAY = 400`。改任何一個都要重算另外兩個。
- **特效素材已經把 alpha 烘進檔案**（`alpha = max(R,G,B)`），Pixi 裡要用加亮混合就設 `sprite.blendMode = 'add'`（v8 用字串，不是舊版的 `BLEND_MODES` 常數），不要重複做一次亮度轉透明。
- **`ParticleContainer` 有一個會影響效果規劃的硬限制：一個容器裡的所有粒子必須共用同一張底圖。**
  所以「火花 + 煙塵 + 碎片」不能塞進同一個容器 —— 要嘛開多個 `ParticleContainer`（各自一張圖），
  要嘛做成一張 texture atlas 再用 `uvs` 切。規劃第 2、5、10 項時要先決定走哪一種。
  另外它用 `addParticle()` 不是 `addChild()`，而且**一定要設 `boundsArea`**，
  否則容器的 bounds 回報為 0 會被 culling 當成不可見整個消失。
- 只有會逐幀變動的屬性要在 `dynamicProperties` 打開（預設只有 `position`）。
  粒子要變色或旋轉就得額外開 `color` / `rotation`，開越多每幀上傳 GPU 的資料越多。
- `unit()` 是量一個隱形探針元素拿到 `--u` 的實際 px 值。Pixi 場景的縮放要跟著這個走，否則在觸控電視上放到最大時粒子會小得像灰塵。
